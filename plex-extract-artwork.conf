{
  // Plex Artwork Extractor configuration
  // Run: python3 extract_artwork.py  (first-run dialogs will populate paths)
  // Or fill in paths manually and run: python3 extract_artwork.py --extract

  // Library roots — use arrays to scan multiple drives / NAS shares
  "movies_library_roots":  [],
  "tv_library_roots":      [],
  "music_library_roots":   [],

  // Single-volume shortcuts (populated automatically if only one path is added per type)
  "movies_library_root":   "",
  "tv_library_root":       "",
  "music_library_root":    ""
}
